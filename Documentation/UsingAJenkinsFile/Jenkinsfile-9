pipeline {
    agent {
        // Define agent details here
    }
    stages {
        stage('Example stage 1') {
            environment {
                BITBUCKET_COMMON_CREDS = credentials('jenkins-bitbucket-common-creds')
            }
            steps {
                echo 'BITBUCKET_COMMON_CREDS_USR and BITBUCKET_COMMON_CREDS_PSW'
            }
        }
        stage('Example stage 2') {
            steps {
                //
            }
        }
    }
}